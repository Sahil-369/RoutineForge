<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/card_task_container"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="horizontal" android:background="@drawable/bg_card"
    android:layout_marginBottom="8dp" android:padding="14dp"
    android:gravity="center_vertical">

    <!-- Time column -->
    <TextView android:id="@+id/tv_task_time"
        android:layout_width="52dp" android:layout_height="wrap_content"
        android:textSize="13sp" android:textColor="@color/accent_orange"
        android:textStyle="bold" android:gravity="center"/>

    <!-- Divider line -->
    <View android:layout_width="1dp" android:layout_height="40dp"
        android:background="#333333" android:layout_marginStart="8dp"
        android:layout_marginEnd="12dp"/>

    <!-- Content -->
    <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:orientation="vertical">

        <TextView android:id="@+id/tv_task_name"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:textSize="15sp" android:textColor="@color/text_primary"
            android:textStyle="bold" android:maxLines="1"
            android:ellipsize="end"/>

        <TextView android:id="@+id/tv_task_description"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:textSize="12sp" android:textColor="@color/text_secondary"
            android:maxLines="1" android:ellipsize="end"
            android:layout_marginTop="2dp" android:visibility="gone"/>

    </LinearLayout>

    <!-- Action buttons -->
    <ImageButton android:id="@+id/btn_task_start"
        android:layout_width="32dp" android:layout_height="32dp"
        android:src="@drawable/ic_play"
        android:background="@drawable/bg_start_button"
        android:layout_marginEnd="6dp" android:padding="5dp"
        android:contentDescription="Start Now" android:visibility="gone"/>

    <ImageButton android:id="@+id/btn_task_edit"
        android:layout_width="32dp" android:layout_height="32dp"
        android:src="@drawable/ic_edit"
        android:background="@android:color/transparent"
        android:layout_marginEnd="2dp" android:padding="4dp"
        android:contentDescription="Edit"/>

    <ImageButton android:id="@+id/btn_task_delete"
        android:layout_width="32dp" android:layout_height="32dp"
        android:src="@drawable/ic_delete"
        android:background="@android:color/transparent"
        android:layout_marginEnd="2dp" android:padding="4dp"
        android:contentDescription="Delete"/>

    <ImageButton android:id="@+id/btn_task_done"
        android:layout_width="36dp" android:layout_height="36dp"
        android:src="@drawable/ic_done_outline"
        android:background="@android:color/transparent"
        android:padding="4dp" android:contentDescription="Mark Done"/>

</LinearLayout>
